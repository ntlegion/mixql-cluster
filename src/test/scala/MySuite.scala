// For more information on writing tests, see
// https://scalameta.org/munit/docs/getting-started.html
class MySuite extends munit.FunSuite {
  test("test converting int to bytes and back") {
//    val i: Int = 164
//    val res = SocketOperations.convertByteArrayToInt(SocketOperations.intToBytes(i))
////    assertEquals(i, r)
//    assert(i == res)
  }
}
